# Images

Place brand assets here (logo, favicon, social preview, etc.).

The app currently renders a typographic `BrandLogo`; to use a real logo file,
add `logo.png` here and point `WebsiteSettings.logoUrl` at it, then swap the
mark inside `lib/core/widgets/brand_logo.dart` for `Image.asset(logoUrl)`.
